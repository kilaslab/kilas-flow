// Package node defines the node contract and the registry.
//
// Nodes are described by metadata (ports, properties, versions) rather than by
// bespoke UI code, so the editor can render every property form from
// GET /api/v1/node-types without hardcoding per-node layouts.
//
// Milestone 1.
package node
