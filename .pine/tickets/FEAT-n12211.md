---
id: FEAT-n12211
title: Declarative credential types in node packs
status: todo
priority: high
labels:
    - saas
    - packs
    - credentials
parent: EPIC-7c3ry9
created: "2026-09-23T02:07:00Z"
updated: "2026-09-23T02:07:00Z"
---

# Description

A host's own pack must authenticate against the host API with a named credential type, for example "Acme API" with a non-secret `baseUrl`, a secret `apiKey`, a header placement and a test request. Today packs can only name built-in types (`docs/src/content/docs/concepts/credentials.md:159-205`: "A pack cannot contribute one today"), although the Go registry is already open (FEAT-2f68r8).

# Acceptance Criteria
- [ ] The pack manifest accepts `credentialTypes[]` with `{id, displayName, fields (property language, secret and masked flags), authenticate (placement descriptor), test}`.
- [ ] Ids are namespaced to the pack and immutable once released.
- [ ] `nodepackgen validate` checks the new block.
- [ ] The types appear in `GET /credential-types` and work in routing and lifecycle requests.
- [ ] The credentials doc is updated.

# Implementation Plan

# Notes

Source: a 2026-09-23 review of a host SaaS application (a multi-tenant customer-messaging and CRM product) that plans to replace its in-house workflow engine and data tables by embedding KilasFlow. Written generically on purpose: any SaaS embedding KilasFlow hits the same gap.

## Audit 2026-09-25 (code vs ticket, main @ 17b6d38)

Valid; one fact misleads. Doc ref credentials.md:159-205 is exact; `Registry.Register` at internal/credentials/registry.go:128; nothing in internal/nodepack parses `credentialTypes[]`.
- Sidecar (n8n community) packages already register credential types at boot (internal/sidecarnode/load.go:182-194, wired at cmd/kilasflow/main.go:403-409) — copy that path, including its duplicate-owner checks. The doc line "nothing in startup registers a credential type" is out of date.
- New types must land in the `credentials.Default()` singleton (registry.go:352-361); lifecycle `call` looks them up there (internal/webhook/request_lifecycle.go:486).

# Related Files

# Attachments
