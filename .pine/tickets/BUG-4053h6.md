---
id: BUG-4053h6
title: 'Expression grammar + semantics: operators/JS/Luxon, functions, $now/$env/$node/$input/$workflow'
status: doing
priority: high
labels:
    - expression
    - n8n-parity
    - full-review
    - wf-c415e773
parent: EPIC-cfe7ny
created: "2026-09-19T12:06:10Z"
updated: "2026-09-20T02:29:41Z"
---

Source: KilasFlow full-review workflow `wf_c415e773-4e1` (Find 14/14 + Verify 14/14 + Critique 1/1). Evidence: live repros against stub/n8n/private instances in `scratchpad/work/<dim>/` (FINDINGS.md, PROGRESS.md) plus journal `wf_c415e773-4e1/journal.jsonl`. Excluded from this epic: 10 verifier-refuted/tracked items (documented bounds, already-open FEAT-1axhdn/FEAT-8mymac halves).

Consolidates 15 finding(s) from dims: find:expression-parity, find:importer-fidelity, find:unfinished-work.

---
### Expression grammar covers only a small subset of what n8n workflows use (no operators, JS methods, Luxon, n8n extension functions, JSON encoding) [find:expression-parity] (critical/parity-gap) · area: expression engine · confidence: high

By design the grammar is: a root, then .field/[i] accessors, then 19 allowlisted calls whose arguments are literals only. Operators, ternaries, ||/??, optional chaining, template literals, regex, arrow functions, JS globals, Luxon and n8n's extension functions are all rejected. There is no way at all to JSON-encode a value, so objects or free text cannot be safely placed in a JSON body.

Evidence: internal/expression/doc.go ('deliberately not a language'). GET /api/v1/expression-grammar lists 11 roots and 19 functions. 90-probe side-by-side run (Set node per probe, same seed item; work/expression-parity/diff.json): 28/90 identical; 41 are KF-only errors, 17 are silent value differences, 3 are n8n-only errors, 1 errors on both. Examples: `$json.count + 1`, `$json.active ? 'yes':'no'`, `$json.missing || 'default'`, `?? `, `?.`, `` `Hello ${...}` ``, `JSON.stringify(...)`, `Object.keys`, `.map(t => ...)`, `.slice`, `$now.toFormat`, `.minus({days:1})`, `DateTime.fromISO`, `.toDateTime()`, `.isEmpty()`, `.extractEmail()`, `.sum()`, `.removeDuplicates()`, `.toJsonString()`, `.toFixed(2)`, `$vars`, `$runIndex`, `$prevNode`, `$items()`, `$jmespath`. All succeed on n8n and fail on KF. Corpus classification of the 205 failing params (classify.py, divergence-classes.json), templates affected

n8n behavior: `{{ }}` bodies are JavaScript expressions with n8n extension functions, Luxon DateTime, $jmespath, $vars and similar helpers. Probe values are in diff.json (e.g. `$json.count + 1` -> 43, `$now.toFormat('yyyy')` -> '2026').

Impact: 205 of 1465 expression params in 48 of 89 templates cannot parse. Counting the runtime divergences in the other findings, only 8 of 89 templates that use expressions avoid every known-divergent construct. toJsonString/JSON.stringify: 25 params in 11 templates, and they are the standard way to build LLM/API JSON bodies.

Suggested fix: Make an explicit product decision and publish a compatibility matrix. Either (a) add a sandboxed JS expression evaluator (e.g. goja with no host bindings, CPU, time and allocation limits, frozen globals and a Luxon-compatible date shim), or (b) greatly widen the allowlisted grammar: binary/logical/comparison operators, ternary, ??, ||, optional chaining, template literals, nested-expression arguments, a Luxon subset (toFormat, plus, minus, startOf, toISO, fromISO) and n8n extension functions (to

Files: internal/expression/expression.go, internal/expression/functions.go, internal/expression/roots.go, internal/expression/doc.go

Existing tickets: FEAT-v8k1tc, FEAT-pn3dtq

---
### Lone {{ $now }} / {{ $today }} leak an opaque Go struct: Set string -> "{}", DateTime and IF nodes reject it as 'not a date' [find:expression-parity] (high/bug) · area: expression engine / date values · confidence: high

A template that is only `{{ $now }}`, `{{ $today }}` or `{{ $now.plusDays(n) }}` returns the evaluator's unexported dateValue struct. The Set node JSON-marshals it to the string "{}". The DateTime node and IF/Filter date conditions only accept time.Time, string or number, so they fail.

Evidence: internal/expression/functions.go:16 (dateValue with unexported field); nodes/assignments.go:101-111 (json.Marshal fallback); internal/datetime/parse.go:60-79; internal/conditions/conditions.go:518-541. Live (t_dates.py -> dates-kf.json vs dates-n8n.json): Set string `{{ $now }}` gives KF "{}" vs n8n "2026-09-19T15:36:41.792+07:00", and the same for $today and $now.plusDays(1). DateTime addToDate with magnitude `{{ $now }}` gives KF 'item 1: 2026-09-19T08:35:38Z is not a date'; n8n returns newDate. IF dateTime 'after' with left `{{ $now }}` gives KF 'condition left value is not a date'; n8n routes true. Template 1744 is one of only 4 templates that activate, and its run fails at node '12 Hours from now' with the same error (exec_01a0b8cc-8f30-7f26-994c-d3a2fa10f477).

n8n behavior: $now/$today are Luxon DateTimes. They serialise as ISO with millis and offset (e.g. 2026-09-19T15:36:41.792+07:00) and are accepted by DateTime and IF date comparisons.

Impact: $now is used in 36 params / 22 templates and $today in 5/3. Schedules, date filters and timestamps written to sheets or DBs are corrupted or fail.

Suggested fix: Never let dateValue escape Evaluate: turn a lone date into time.Time (or an ISO string with millis and offset) before returning. Teach datetime.Parse and conditions.asTime to accept it. Add tests for Set/DateTime/IF fed from `{{ $now }}`.

Files: internal/expression/functions.go, internal/expression/expression.go, nodes/assignments.go, internal/datetime/parse.go

Existing tickets: FEAT-v8k1tc

---
### Allowlisted functions share JS names but not JS semantics (replace, includes, stringification, escapes, .length, join) [find:expression-parity] (high/bug) · area: expression engine / functions · confidence: high

Several allowlisted functions silently give different results from n8n under the same name. replace replaces every occurrence instead of the first. includes() on arrays compares stringified text. Objects and arrays placed in text render with Go %v syntax. Single-quoted arguments keep backslash escapes. .length works only as a KF-only call. join() requires a separator.

Evidence: functions.go:61 (replace = strings.ReplaceAll), :70-83 (includes compares stringify(entry) == args[0].text), :114 (join arity 1), :85 (length as a call); expression.go:644-665 (stringify default fmt.Sprintf("%v")). Live diff.json: p20 `email.replace('a','A')` n8n 'AdA.Lovelace@Example.com' vs KF 'AdA.LovelAce@ExAmple.com'. p90 `replace('.', '_')` n8n 'Ada_Lovelace@Example.com' vs KF 'Ada_Lovelace@Example_com'. p23 `[3,1,2,2].includes(2)` n8n true vs KF false; p87 includes('2') n8n false vs KF true. p12 'Tags: {{ $json.tags }}' n8n 'Tags: alpha,beta,gamma' vs KF 'Tags: [alpha beta gamma]'. p11 object renders as 'map[city:London ...]'. p64 `join('\n- ')` gives a literal backslash-n on KF. p84 `join()` gives KF error 'join() takes 1 argument(s), got 0'. p14/p15/p80 `.length` gives '$json.tags.length is not an object', while p16 `.length()` is rejected by n8n. harness/edge: `replace('', '-')

n8n behavior: Standard JavaScript String/Array semantics (see the probe values above).

Impact: The results are silently wrong, with no error. replace and includes appear in many parse-OK params. .length is used in 10 params / 6 templates. Arrays interpolated into prompts and messages change format.

Suggested fix: Match JS semantics for every function that keeps a JS name: replace = first occurrence (add replaceAll), includes with type-strict equality for numbers and bools, join default ',', `.length` as a property, JS escape handling in single-quoted literals, and JS-like stringification (arrays comma-joined, objects as JSON rather than Go syntax). Add a parity table test.

Files: internal/expression/functions.go, internal/expression/expression.go

Existing tickets: FEAT-v8k1tc

---
### Legacy $node["X"].json always returns the FIRST item (silently wrong on multi-item flows) [find:expression-parity] (high/bug) · area: expression engine / $node root · confidence: high

`$node["X"].json.field` gives the same first item of X for every current item, with no error, where n8n returns the corresponding item.

Evidence: runner.go nodeItemFor sets item.JSON = Items[0]; expression.go:541-565 merges item.JSON into the $node wrapper. Live batch2 (batch2.py, batch2-n8n.json vs batch2-kf.json) over 3 items from Split Out: q01 `{{ $node["Split Out"].json.v }}` gives n8n a,b,c vs KF a,a,a.

n8n behavior: `$node["X"].json` returns the item of X that matches the current item (a,b,c in the probe).

Impact: 16 params in 6 templates (14 use .json), e.g. 1748 and 1750 (1750 is one of the 4 activatable templates). The data is silently wrong, which is worse than a failure.

Suggested fix: Resolve `$node["X"].json` with the same paired-item or item-index logic as `$('X').item`, or fail with a pointed message when the item is ambiguous. Never silently use item 0.

Files: internal/engine/runner.go, internal/expression/expression.go

Existing tickets: FEAT-v8k1tc

---
### $env is always empty: KILASFLOW_WORKFLOW_ENV_* wiring was dropped, and unknown keys resolve silently to "" [find:expression-parity] (high/bug) · area: expression engine / $env root / boot wiring · confidence: high

The `$env` allowlist (KILASFLOW_WORKFLOW_ENV_*) is documented, but the engine never receives it. workflowEnvironment() is dead code, so every `$env.X` resolves to undefined, i.e. '' in text, with no error.

Evidence: cmd/kilasflow/main.go:933-951 defines workflowEnvironment() but nothing calls it. engine.ServiceDeps.Environment (internal/engine/service.go:71-74) is not set in main.go:422-440. `git show db65aa1 -- cmd/kilasflow/main.go` shows `-\t\tEnvironment: workflowEnvironment(),` removed in the FEAT-rj17xj/knpfqf/gjzgkd merge. FEAT-pn3dtq's live proof used `$env.REGION`. Docs still promise it: docs/src/content/docs/concepts/expressions.md:144, operate/configuration.md:44, concepts/safety-boundaries.md:210. Live p86 `{{ $env.HOME }}` gives KF '' (silent). n8n 2.33.7 raises 'access to env vars denied' (blocked by default).

n8n behavior: $env reads process env when allowed, and otherwise fails loudly with 'access to env vars denied'.

Impact: 15 params in 5 templates (e.g. `$env["GOOGLE_API_KEY"]`, `$env.TELEGRAM_CHAT_ID`) and any embedder that relies on the documented env allowlist. API keys and IDs are sent empty instead of failing.

Suggested fix: Restore `Environment: workflowEnvironment()` in the ServiceDeps literal and add a boot-wiring test. Make a missing `$env` key a clear error (or at least a warning) instead of silent undefined.

Files: cmd/kilasflow/main.go, internal/engine/service.go, internal/expression/expression.go

Existing tickets: FEAT-pn3dtq, FEAT-knpfqf

---
### $input API is KilasFlow-specific: $input.item silently undefined, $input.first()/last()/all() error, $('X').params silently empty [find:expression-parity] (high/parity-gap) · area: expression engine / $input and node accessors · confidence: high

KF's $input is a map of port to items (`$input.main[0].x`). n8n's API ($input.item, .first(), .last(), .all(), .params) does not exist. `.item` and `.params` resolve to undefined without an error. `.isExecuted` and `.itemMatching()` are also missing.

Evidence: expression.go:531-540 (rootValue $input); internal/expression/expression_test.go:44 uses `$input.main[1].name`. Live batch2: q05 `$input.item.json.v` gives n8n a,b,c vs KF '' (silent). q06/q07/q14 `$input.first()/all()[2]/last()` give KF 'first() needs a list or a node'. q10 `$('Split Out').isExecuted` gives n8n true vs KF type error. q11 `$('Split Out').params.fieldToSplitOut` gives n8n 'list' vs KF '' (silent).

n8n behavior: $input.item is the current input item; $input.first()/last()/all() read the node's main input; $('X').params gives the node's parameters; isExecuted is a boolean.

Impact: $input.all 9 params / 7 templates and $input.item 3 / 2 in the template corpus. $input is also the dominant idiom in n8n docs and in user-written expressions, and the silent cases produce empty data.

Suggested fix: Implement n8n's $input surface on the current node's input (item = current item, first/last/all over the main input, params = own parameters) while keeping `$input.main[...]` for back-compat. Add `$('X').isExecuted`, `.params` and `.itemMatching(i)`.

Files: internal/expression/expression.go, internal/expression/roots.go, internal/engine/authenticate.go

Existing tickets: FEAT-v8k1tc

---
### $workflow.id / .name / .active are always empty or false [find:expression-parity] (medium/bug) · area: expression engine / $workflow root · confidence: high

engine.Request.Workflow is never filled in production, so `$workflow` exposes an empty identity. In the corpus it is used exclusively for self-calls: toolWorkflow/executeWorkflow workflowId = `{{ $workflow.id }}`.

Evidence: internal/engine/runner.go:155 declares Request.Workflow. The only assignment is in runner_test.go:1366, and authenticate.go:99 passes the zero value. Live p49 `{{ $workflow.name }}|{{ $workflow.active }}` gives KF '|false' vs n8n '[expression-parity] n8n probes|true'. batch2 q12 `{{ $workflow.id }}|{{ $workflow.name }}` gives KF '|' vs n8n '2vOjv7Dn6TIZgYpd|[expression-parity] batch2'. $execution.mode is 'manual' where n8n reports 'production' or 'test', and $execution.id is 'exec_<uuid>', which breaks `$execution.id.toNumber()` (template 1534).

n8n behavior: $workflow.id, .name and .active reflect the running workflow. $execution.mode is 'test' or 'production'.

Impact: 27 params in 10 templates, all in workflowId of toolWorkflow (19+2+1) or executeWorkflow (4+1). Sub-workflow tools that call their own workflow resolve to an empty id.

Suggested fix: Fill Request.Workflow {ID, Name, Active} wherever an execution is started (manual run, webhook, schedule, sub-workflow, resume) and add a test through the service. Document the $execution.mode values.

Files: internal/engine/runner.go, internal/engine/authenticate.go, internal/engine/service.go

Existing tickets: FEAT-v8k1tc

---
### $now/$today ignore the workflow timezone (always UTC) and stringify without millis [find:expression-parity] (medium/bug) · area: expression engine / dates · confidence: high

The engine never sets expression.Context.Now, so $now is time.Now().UTC(). $today is the start of the UTC day even when the workflow carries settings.timezone, which the importer preserves. Mixed-text rendering drops milliseconds and the offset.

Evidence: internal/engine/authenticate.go:94-103 (no Now/location); expression.go:616-621 clock() returns time.Now().UTC(). Live (t_tz.py) with settings.timezone America/Los_Angeles (kept by the importer): KF 'T 2026-09-19T00:00:00Z', 'N 2026-09-19T08:45:05Z' vs n8n 'T 2026-09-19T00:00:00.000-07:00', 'N 2026-09-19T01:45:06.127-07:00'. The same happened with the n8n instance's default +07:00 zone (p38, p68).

n8n behavior: $now/$today use the workflow timezone setting, else the instance GENERIC_TIMEZONE.

Impact: $today falls on the wrong calendar day for part of every day outside UTC. Date-bucketed reports, 'today's' queries and filenames are affected. $now is in 22 templates and $today in 3.

Suggested fix: Build Context.Now in the workflow's timezone (settings.timezone, then instance default, then UTC) and compute $today in that zone. Render dates the way n8n does (ISO with millis and offset).

Files: internal/engine/authenticate.go, internal/expression/expression.go, internal/expression/functions.go

Existing tickets: FEAT-q81bq4, FEAT-v8k1tc

---
### $now.format() takes Go layouts, not Luxon tokens, so imported patterns silently return the pattern text [find:expression-parity] (medium/bug) · area: expression engine / date formatting · confidence: high

format(...) calls Go's time.Format. An imported Luxon pattern like 'yyyy-MM-dd' comes back unchanged as the literal 'yyyy-MM-dd', and '2006' is turned into the current year. The difference is documented but neither flagged at import nor refused. KF-only helpers (plusDays, minusDays, toISOString) also break when exported to n8n.

Evidence: functions.go:152-158. Live p36 `$now.format('yyyy')` gives n8n '2026' vs KF 'yyyy'. p37 `format('2006')` gives n8n '2006' vs KF '2026'. harness/edge `format('yyyy-MM-dd')` gives 'yyyy-MM-dd'. p40 `$now.plusDays(1).format('2006-01-02')` and p82 `toISOString()` give null on n8n. Documented at docs/src/content/docs/concepts/expressions.md:176-179, and doc.go uses `$now.plusDays(7).format("2006-01-02")` as the example.

n8n behavior: DateTime.format/toFormat use Luxon tokens (yyyy, MM, dd, LLL...).

Impact: .format( appears in 8 params / 6 templates (e.g. `$today.plus({days: 14}).format("d. MMM. y")`, `$now.format('dd/LL/yyyy')`), and every exported KF workflow that uses format or plusDays breaks on n8n.

Suggested fix: Accept Luxon tokens in format/toFormat (translate the token set). At minimum, refuse or flag patterns that contain Luxon tokens instead of echoing them. Rename KF-only helpers or add n8n-compatible names (plus/minus with a duration object).

Files: internal/expression/functions.go, internal/expression/doc.go, docs/src/content/docs/concepts/expressions.md

Existing tickets: FEAT-v8k1tc

---
### URLs built from expressions are sent without percent-encoding, and upstream rejects them [find:expression-parity] (medium/bug) · area: HTTP Request node / expression interpolation · confidence: high

After interpolation the URL goes through url.Parse unchanged. The query is re-encoded only when sendQuery is on, so a value with a space or other reserved character yields an invalid request line.

Evidence: nodes/http.go:224-237. Live (t_http.py url_expr): url `=http://127.0.0.1:8092/echo?v={{ $json.x }}` with x='hello world' gives KF 'request failed with status 400' (the raw space is on the request line). n8n requests /echo?v=hello%20world and succeeds.

n8n behavior: n8n (axios) percent-encodes invalid characters in the resolved URL.

Impact: 91 HTTP URLs in 33 templates are built from expressions, e.g. template 2431 `https://www.google.com/search?q=site:{{ $json['Website Domaine'] }}+{{$json.Subject}}` and template 1750's Google URL. Any value containing spaces, accents or quotes fails.

Suggested fix: Normalise the URL after interpolation: re-encode the path and query components that contain invalid characters, like browsers and axios do, without double-encoding existing %XX.

Files: nodes/http.go

Existing tickets: FEAT-pn3dtq

---
### Out-of-range indexes and field reads on scalars hard-fail where n8n yields undefined [find:expression-parity] (low/bug) · area: expression engine / undefined semantics · confidence: high

Soft-undefined applies only to missing object keys. An index past the end of a list, or a field read on a string or number, fails the whole node.

Evidence: expression.go:267-291 ('is not an object', 'is out of range'). Live p76 `$json.tags[5]` gives n8n null vs KF '$json.tags[5] is out of range'. batch2 q15 `$('Split Out').all()[5]` behaves the same. p75 `$json.name.missingProp` and p77 `$json.count.foo` give n8n null vs a KF error.

n8n behavior: Returns undefined (null in output) for these reads.

Impact: 131 parse-OK params in 28 templates use numeric indexes (e.g. `$json.messages[0]`, `.split(' ')[1]`, `$json.data[0].url`). A short list aborts the run.

Suggested fix: Return Undefined for out-of-range indexes and for property reads on scalars, matching JS. Keep hard errors for real grammar problems.

Files: internal/expression/expression.go

Existing tickets: FEAT-v8k1tc

---
### Expression parser is quote-unaware for ')' and ']' inside string literals and ASCII-only for identifiers [find:expression-parity] (low/bug) · area: expression engine / parser · confidence: high

Paren-depth counting in readCallArguments and the ']' scan in parseAccessors ignore quotes, and isNameByte accepts ASCII only. Valid expressions are rejected.

Evidence: internal/expression/expression.go:382 (parseAccessors: IndexByte ']'), :448 (readCallArguments depth loop), :520 (isNameByte). harness/edge: `$json.a.split(')')` gives 'expression argument "'" must be a quoted string or a number'. `$json.a.replace('(', '[')` gives 'expression has an unclosed ('. `$json['a]b']` gives 'expression index must be a number or a quoted key'. `$json.título` gives 'unsupported syntax at "ítulo"'. Template 4352 `{{ $json['choices[0].message.content'] }}` fails for this reason.

n8n behavior: Standard JS tokenisation.

Impact: Small corpus impact (2 params), but it also affects node names and keys containing ')' or ']' and non-English field names.

Suggested fix: Tokenise with quote awareness when scanning for ')' and ']', and accept Unicode letters in identifiers.

Files: internal/expression/expression.go

---
### Typed Set coercion differs from n8n for null: 'null' type imported as string ("{}"), and undefined in string rows becomes "" [find:expression-parity] (low/bug) · area: importer / Set assignment types · confidence: high

n8n Set v3.4 supports a 'null' assignment type. The importer silently changes it to string, so the value {} becomes the text "{}". A lone undefined or null expression in a string row becomes "" instead of null.

Evidence: internal/interop/n8n/parameters.go:173-183 (assignmentTypeOf maps unknown types to string, no issue raised); nodes/assignments.go:101-104 (nil -> ""). Live template 5170 run: json_example_null = "{}" (n8n null). diff.json p08 `{{ $json.missing }}` and p72 `{{ $json.profile.none }}` give n8n null vs KF ''.

n8n behavior: The 'null' type yields null. A string row whose expression resolves to null or undefined yields null.

Impact: Downstream IF 'is empty/is null' checks and JSON payloads get a string instead of null (5170 tutorial, plus any workflow with optional fields).

Suggested fix: Support a null assignment type, or at least flag the conversion as lossy. Keep null for string rows when the expression resolves to null or undefined.

Files: internal/interop/n8n/parameters.go, nodes/assignments.go

Existing tickets: FEAT-xqqjqv

---
### Sub-workflow references: `$workflow.id` self-calls are refused or garbled, and foreign n8n workflow IDs in Workflow Tools are carried silently [find:importer-fidelity] (high/bug) · area: importer/executeWorkflow + toolWorkflow · confidence: high

(a) A toolWorkflow with workflowId `={{ $workflow.id }}` is stored as an expression object {mode:expression}, not a locator, so activation fails with the misleading 'workflowId is required' and no import issue is raised. (b) executeWorkflow with `$workflow.id` gets the blocking 'identified by its n8n workflow ID, which does not exist on this server', although a self-reference always resolves. (c) A toolWorkflow pointing at another n8n workflow ID is stored as a bare string with no import issue and no activation error, and cachedResultName (the target workflow's name) is discarded.

Evidence: (a) 7 templates / 20 tool nodes: 2006, 2026, 2035, 2085, 3135, 3514, 3770. For example, 2026's tool params -> workflowId {"mode":"expression","value":"{{ $workflow.id }}"}, and activation reports 'configuration is invalid: workflowId is required'. The inline-JSON tools in 2315 and 2324 also show the same misleading error. (b) 1534, 2567, 2878 (x3). (c) 2563, 2606, 2783 (7 tools), 2786, 3050, 3443 and 3790: imported as e.g. 'nRGs7Ogv7eU1mJQl' with issues [] and no activation error, so the failure only appears when the agent calls the tool.

n8n behavior: workflowId is a resourceLocator {__rl, mode, value, cachedResultName}; `$workflow.id` evaluates to the running workflow's own ID.

Impact: 17/100 templates. Self-calling tools are the dominant way templates build agent tools.

Suggested fix: Share one locator translator between both nodes: keep {__rl, mode:id, value:<expression>}, and treat `$workflow.id` as a self-reference (no blocking issue, or rewrite it to the new ID). For literal foreign IDs, raise the same blocking issue on tools as on executeWorkflow and include the cached name in the reason.

Files: internal/interop/n8n/parameters.go, nodes/ai.go, nodes/subworkflow.go

Existing tickets: FEAT-347egc (done), FEAT-az620p (done)

---
### Regression: $env is always empty because merge db65aa1 dropped the Environment wiring [find:unfinished-work] (high/bug) · area: engine / expressions · confidence: high

`{{ $env.X }}` never resolves. The only code that builds the KILASFLOW_WORKFLOW_ENV_* allowlist is no longer passed to the engine, so the engine gets an empty map even when the variables are set.

Evidence: `git show db65aa1 -- cmd/kilasflow/main.go` removes `Environment: workflowEnvironment(),`. workflowEnvironment() at cmd/kilasflow/main.go:938 now has zero callers, and main.go no longer sets `Environment:` at all. internal/engine/service.go:71-74,144 copies deps.Environment (nil) into every run, so the `$env` handler at internal/expression/expression.go:566 always reads an empty map. Five doc pages still document the allowlist (operate/security.md:59, operate/configuration.md:44, concepts/expressions.md:144, concepts/safety-boundaries.md:210, configuration-reference.md:18). Not checked live: the shared server sets no KILASFLOW_WORKFLOW_ENV_* variable and I had no private port for my own instance.

n8n behavior: $env exposes the process environment, unless N8N_BLOCK_ENV_ACCESS_IN_NODE is set.

Impact: Any workflow that reads configuration from $env gets undefined values, including 5 of the 100 templates (e.g. 2982 uses chatId={{ $env.TELEGRAM_CHAT_ID }}). This is a documented security and configuration feature that is broken.

Suggested fix: Restore `Environment: workflowEnvironment()` in the engine deps. Add an integration test that sets KILASFLOW_WORKFLOW_ENV_FOO and evaluates {{ $env.FOO }}. Add staticcheck U1000 to CI.

Files: cmd/kilasflow/main.go, internal/engine/service.go, internal/expression/expression.go

Existing tickets: FEAT-pn3dtq

## Acceptance criteria

- [ ] Expression grammar covers only a small subset of what n8n workflows use (no operators, JS methods, Luxon, n8n 
- [ ] Lone {{ $now }} / {{ $today }} leak an opaque Go struct: Set string -> "{}", DateTime and IF nodes reject it a
- [ ] Allowlisted functions share JS names but not JS semantics (replace, includes, stringification, escapes, .lengt
- [ ] Legacy $node("X").json always returns the FIRST item (silently wrong on multi-item flows)
- [ ] $env is always empty: KILASFLOW_WORKFLOW_ENV_* wiring was dropped, and unknown keys resolve silently to ""
- [ ] $input API is KilasFlow-specific: $input.item silently undefined, $input.first()/last()/all() error, $('X').pa
- [ ] $workflow.id / .name / .active are always empty or false
- [ ] $now/$today ignore the workflow timezone (always UTC) and stringify without millis
- [ ] $now.format() takes Go layouts, not Luxon tokens, so imported patterns silently return the pattern text
- [ ] URLs built from expressions are sent without percent-encoding, and upstream rejects them
- [ ] Out-of-range indexes and field reads on scalars hard-fail where n8n yields undefined
- [ ] Expression parser is quote-unaware for ')' and ')' inside string literals and ASCII-only for identifiers
- [ ] Typed Set coercion differs from n8n for null: 'null' type imported as string ("{}"), and undefined in string r
- [ ] Sub-workflow references: `$workflow.id` self-calls are refused or garbled, and foreign n8n workflow IDs in Wor
- [ ] Regression: $env is always empty because merge db65aa1 dropped the Environment wiring
- [ ] Adversarial re-verify against live stub/n8n like the Verify phase (no code-only close)
## Progress — ExpressionParity slice (internal/expression, internal/engine/authenticate.go)

Scope owned by this slice: `internal/expression/*.go` and
`internal/engine/authenticate.go`. Findings whose fix lives in another slice's
files are listed with the owner that was asked, not silently dropped.

### Landed (commit 1 — expression engine rewrite)

- **Grammar (critical)**: replaced the root+accessors grammar with a
  tokeniser/parser/evaluator for the JavaScript expression subset. New syntax:
  binary/arithmetic/comparison/equality operators, ternary, `??`, `&&`, `||`,
  optional chaining `?.`, template literals, array and object literals, spread
  in call arguments, arrow functions, unary `!`/`-`/`+`, and expression
  arguments in calls. `internal/expression/parser.go`, `evaluator.go`.
- **JS semantics**: `+` (text vs arithmetic), truthiness, `==` vs `===`,
  ordering, `??` looking past null/undefined only, NaN/Infinity rendering.
- **Function semantics**: `replace` = first occurrence, new `replaceAll`,
  `includes` = SameValueZero (not stringified), `join` defaults to `,`,
  `.length` is a property (and `.length()` is refused), single-quoted escapes
  are JS escapes, lists join with commas in text and objects render as JSON.
- **JSON encoding**: `JSON.stringify` (with indent), `JSON.parse`,
  `Object.keys/values/entries/fromEntries`, `Array.isArray/from`,
  `Math.*` (incl. `min`/`max`/`hypot`/`PI`), `Number/String/Boolean/parseInt/
  parseFloat/isNaN/encodeURIComponent/decodeURIComponent`, `toJsonString()`.
- **List methods**: `map`, `filter`, `find`, `findIndex`, `some`, `every`,
  `reduce`, `flat`, `flatMap`, `reverse`, `concat`, `slice`, `at`, `sum`,
  `removeDuplicates`/`unique`, `count`, `first`/`last`/`all`.
- **Dates**: `$now`/`$today` stringify as ISO 8601 with milliseconds and an
  offset; a lone `{{ $now }}` is returned as `time.Time` (no more `{}` in a Set
  node, no more "not a date" in DateTime/IF); a nested date marshals via
  `MarshalJSON`. Luxon tokens for `format`/`toFormat` (yyyy, MM, dd, LLL, EEEE,
  Z, …) instead of Go layouts; `plus`/`minus` with a duration object or a unit;
  `startOf`/`endOf`; `toISO`/`toISODate`/`toISOString`/`toMillis`/`toSeconds`/
  `toJSDate`/`setZone`/`diff`; Luxon getters (`$now.year`, `$now.weekday`);
  `DateTime.now/local/utc/fromISO/fromMillis/fromSeconds/fromFormat/fromJSDate`.
- **Timezone**: `expression.Context.Timezone` (new) is read by the clock;
  `authenticate.go` fills it from `Request.Workflow.Timezone`, so `$now`/
  `$today`/`DateTime.local()` are in the workflow's zone.
- **Undefined semantics**: an index past the end of a list or a string, and a
  field read on a scalar, are undefined instead of a hard failure. A string
  index returns the character, as JavaScript does.
- **Parser robustness**: quote-aware scanning for `)`/`]` inside string and
  template literals, Unicode identifiers (`$json.título`), escaped quotes.
- **$env**: reads the allowlist map; a key that is not there is a loud error
  naming `KILASFLOW_WORKFLOW_ENV_<KEY>` instead of resolving to `""`.
- **$input**: n8n's API added on top of the port map — `.item`, `.first()`,
  `.last()`, `.all()`, `.isExecuted`; `$items('Name')` added.
- **$node / $('X')**: `$node["X"].json` reads the paired item when the runtime
  established lineage and otherwise the item at the same position (never the
  first item for every current item). `.params` and `.isExecuted` added;
  `NodeItem.ItemOrigins` + `expression.OriginKey(...)` exported for the runtime's
  pairing; `.item` still refuses to guess.
- **Roots**: `$vars`, `$runIndex`, `$items` added; `Roots()`/`FunctionNames()`
  still serve the editor.

### Deferred to the owning slice (asked, not edited here)

- `cmd/kilasflow/main.go`: `Environment: workflowEnvironment()` must be restored
  in the ServiceDeps literal (SecurityFront2). Without it `$env` is an empty
  allowlist and every `$env.X` now fails loudly.
- `internal/engine/runner.go` + service: `request.Workflow` (ID/Name/Active/
  Timezone), `NodeItem.Parameters`, `NodeItem.ItemOrigins`, and
  `PairNodeItems` (EngineFlow). `$workflow.*` is empty until that lands; the
  `Timezone` field is already read here.
- `nodes/http.go`: percent-encode the interpolated URL (WebhookParity).
- `nodes/assignments.go` + `internal/interop/n8n/parameters.go`: the `null`
  assignment type and null-for-undefined in string rows (ImporterTail).
- `nodes/webhook.go` + `internal/webhook`: `$response` must not stay in the
  item JSON that flows downstream (WebhookParity).
- `$jmespath` is refused with a message naming the alternatives rather than
  implemented; it would need a dependency this slice may not add.

## Progress — ExpressionParity slice, final state (testing)

Commits (mine, this ticket): `255c94a` (engine rewrite), `755e5cc` (date
JSON-encoding proof), `aace174` (FunctionNames serves namespaces, deduped),
`d26f242` (ExpressionContext: resume links + timezone + paired lineage),
`6be5d19` (common JS method names), `6275350` (Number/String/Boolean namespace
+ function).

Scoped tests run and their result:

- `go test ./internal/expression/ -count=1` → **ok** (existing suite updated to
  the new contract + `parity_test.go`, which encodes the review's probe values:
  `$json.count + 1` → 43, `replace('a','A')` → `AdA…`, `[3,1,2,2].includes(2)`
  → true / `includes('2')` → false, `Tags: {{ $json.tags }}` →
  `Tags: alpha,beta,gamma`, `$now.format('yyyy')` → 2026, `$today` in
  `America/Los_Angeles` → `2026-09-19T00:00:00.000-07:00`,
  `$node["Split Out"].json.v` → a/b/c per item, `$json.tags[5]` → null,
  `$json.a.split(')')`, `$json['a]b']`, `$json.título`).
- `go test ./internal/engine/ -count=1` → **ok** (includes the new
  `TestExpressionContextCarriesEveryRootItPromises` and EngineFlow's
  `TestDollarItemFollowsPairedLineagePerItem`).
- `go test ./internal/routing/ -count=1` → **ok** (the routing interpreter's
  `$value`/`$parameter`/`$credentials` templates still resolve).

Findings closed inside this slice (files owned: `internal/expression/*.go`,
`internal/engine/authenticate.go`): the grammar gap (operators, ternary, `??`,
`||`, optional chaining, template literals, array/object literals, arrow
functions, spread), JS function semantics (`replace`/`replaceAll`, `includes`,
`join`, `.length`, escapes, stringification), JSON encoding
(`JSON.stringify/parse`, `toJsonString`, `Object.keys/…`), the date findings
(Luxon tokens, milliseconds + offset, no struct leak — a lone `{{ $now }}` is a
`time.Time` that the Set/DateTime/IF nodes accept, timezone from
`settings.timezone` then the instance default), undefined semantics
(out-of-range index, scalar field reads, string indexing), the quote-unaware /
ASCII-only parser, `$env` (allowlist read; a missing key is a loud error naming
`KILASFLOW_WORKFLOW_ENV_<KEY>`), `$input` (n8n API on top of the port map;
`$items('Name')`), `$node`/`$('X')` (`.json` follows the current item — paired
first, then position; `.params`/`.isExecuted`; `.item` still refuses to guess),
`$workflow` (populated by EngineWaits in `service.newRequest`), `$vars`/
`$runIndex`, and the `$execution.resumeUrl`/`approvalUrl` roots that were being
dropped in `ExpressionContext`.

Still open, owned by other slices (asked by hub message, not edited here):

- `cmd/kilasflow/main.go` — restore `Environment: workflowEnvironment()` in the
  ServiceDeps literal (SecurityFront2). `workflowEnvironment()` at main.go:964
  still has no caller, so the allowlist is empty; `$env.X` now fails loudly
  naming the variable instead of sending an empty secret.
- `nodes/http.go` — percent-encode the interpolated URL (WebhookParity).
- `nodes/webhook.go` + `internal/webhook` — `$response` must not stay in the
  item JSON that flows downstream (WebhookParity).
- `nodes/assignments.go` + `internal/interop/n8n/parameters.go` — the Set `null`
  assignment type and null-for-undefined in string rows (ImporterTail).
- `internal/interop/n8n/parameters.go` + `nodes/ai.go` + `nodes/subworkflow.go`
  — `$workflow.id` self-references and foreign workflow IDs in toolWorkflow
  (ImporterTail/AINodes2); the expression half (`$workflow.id` resolving) is
  done.
- `docs/**` — the expression-grammar page and the two `format("2006-01-02")`
  examples are stale; facts dumped to `local://expression-grammar-facts.md`
  (DXOps2).
- `web/src/lib/workflow-editor/expression-grammar.ts` — the shape validator's
  comment claims there is no JSON/Luxon surface, which is no longer true
  (FrontendCore3); the logic itself accepts the new names because
  `FunctionNames()` now includes namespace members.

Not implemented, deliberately: `$jmespath` (needs a dependency this slice may
not add) — it is registered and refuses with a message naming the alternatives.

### Verification note (tree actively edited by siblings)

- `go test ./internal/expression/ -count=1` → ok (re-run after every sibling
  landing).
- `go test ./internal/routing/ -count=1` → ok.
- `go test ./internal/engine/ -count=1` → was fully green when this slice
  landed (`d26f242`), including `TestExpressionContextCarriesEveryRootItPromises`
  and EngineFlow's `TestDollarItemFollowsPairedLineagePerItem`. In the current
  tree the package still has three failures owned by other slices
  (`TestRunWithNoNamedTriggerRunsEveryRoot`,
  `TestRetryAndContinueOnFailComposeSoTheBudgetIsSpentFirst`,
  `TestAnIfInsideALoopReachesATerminalStateWithEveryPrunedRowRecorded`) plus a
  transient `describeRuns` build break in `wait_service_test.go`; the
  expression-related tests in that package pass in the same tree
  (`go test ./internal/engine/ -run
  'TestExpressionContextCarriesEveryRootItPromises|TestDollarItemFollowsPairedLineagePerItem|TestDollarNode'
  -count=1` → ok).

### Verification update (2026-09-20, tree at 4cc80ff)

- `go test ./internal/expression/ -count=1` → ok.
- `go test ./internal/routing/ -count=1` → ok.
- `go test ./internal/engine/ -run
  'TestAuthenticate|TestARedirectOutsideTheScope|TestExpressionContextCarriesEveryRootItPromises|TestDollarItemFollowsPairedLineagePerItem|TestDollarNode'
  -count=1` → ok (all five pass).
- `go test ./internal/engine/ -count=1` → one remaining failure, in a sibling's
  area: `TestServiceProjectsDatastoreOutputFromTrace`
  (`trace_test.go:192`, live event data null vs the persisted summary) — trace
  projection, not expression. It failed and passed alternately while siblings
  were landing engine commits (`1d8191e`, `4cc80ff`), so it is their in-flight
  state, not this ticket.

## Work Evidence

Closed by `pine close --evidence` on 2026-09-20.

- Base: `bf802ab8` (last commit at or before ticket created 2026-09-19)
- Commits (14):
  - `488d9f37` — BUG-4053h6: refresh scoped verification against tree 4cc80ff
  - `4e596006` — BUG-4053h6: percent-encode an interpolated URL query so a resolved value reaches the upstream — http
  - `b53bd701` — BUG-4053h6: note scoped verification against the actively edited tree
  - `f06e439b` — BUG-4053h6: expression pages stop claiming the grammar is not a language — docs
  - `7c9b5632` — BUG-4053h6: expression shape validator comment matches the rewritten engine surface — preview
  - `827e51ad` — BUG-4053h6: document the rewritten expression surface — docs
  - `693a4747` — BUG-4053h6: record testing state — expression parity slice landed with scoped proof
  - `6275350c` — BUG-4053h6: Number/String/Boolean are namespaces and functions — expression
  - `6be5d199` — BUG-4053h6: cover common JS method names (concat/sort/toJSON/...) — expression
  - `d26f2425` — BUG-4053h6: carry resume links, timezone and paired lineage in ExpressionContext — engine
  - `aace174b` — BUG-4053h6: serve namespace members in FunctionNames, deduped — expression
  - `755e5cc7` — BUG-4053h6: prove resolved dates survive JSON encoding (Set-node path) — expression
  - `255c94ae` — BUG-4053h6: n8n-parity expression engine — JS operators/functions, Luxon dates, roots ($env//$node/$workflow) — expression
  - `b4f21475` — chore(pine): track EPIC-cfe7ny full-review remediation backlog (50 tickets)
- Files changed (base → working tree):

```
 .env.example                                       |    2 +-
 .github/workflows/ci.yml                           |   76 +-
 .github/workflows/release.yml                      |    3 +-
 .gitignore                                         |   11 +-
 .pine/tickets/BUG-1tj5wy.md                        |  499 ++++
 .pine/tickets/BUG-277a2m.md                        |  506 ++++
 .pine/tickets/BUG-341sxn.md                        |   40 +
 .pine/tickets/BUG-4053h6.md                        |  461 ++++
 .pine/tickets/BUG-57n76x.md                        |  127 +
 .pine/tickets/BUG-66es9z.md                        |   60 +
 .pine/tickets/BUG-6as5y7.md                        |  220 ++
 .pine/tickets/BUG-6bqh51.md                        |  189 ++
 .pine/tickets/BUG-6jvcs5.md                        |  238 ++
 .pine/tickets/BUG-8dmp5y.md                        |  183 ++
 .pine/tickets/BUG-8h4yy1.md                        |   46 +
 .pine/tickets/BUG-8sb0jw.md                        |  239 ++
 .pine/tickets/BUG-8t94wn.md                        |  179 ++
 .pine/tickets/BUG-9853ay.md                        |   84 +
 .pine/tickets/BUG-9x3te4.md                        |   23 +
 .pine/tickets/BUG-a1648n.md                        |   29 +
 .pine/tickets/BUG-a9mp5a.md                        |  Bin 0 -> 10476 bytes
 .pine/tickets/BUG-aede06.md                        |  326 +++
 .pine/tickets/BUG-c241hm.md                        |  154 ++
 .pine/tickets/BUG-cq4yk3.md                        |  338 +++
 .pine/tickets/BUG-dndnhn.md                        |   48 +
 .pine/tickets/BUG-esb9sh.md                        |  138 ++
 .pine/tickets/BUG-f9frth.md                        |  410 +++
 .pine/tickets/BUG-fv5fer.md                        |  172 ++
 .pine/tickets/BUG-gaavr5.md                        |  363 +++
 .pine/tickets/BUG-hfhzq6.md                        |   49 +
 .pine/tickets/BUG-hm76dq.md                        |  119 +
 .pine/tickets/BUG-j7rtv3.md                        |   90 +
 .pine/tickets/BUG-kzkvv6.md                        |   74 +
 .pine/tickets/BUG-mewhrd.md                        |   68 +
 .pine/tickets/BUG-mz8xrb.md                        |   56 +
 .pine/tickets/BUG-npfz43.md                        |   38 +
 .pine/tickets/BUG-pwckhd.md                        |   63 +
 .pine/tickets/BUG-qmgz2f.md                        |  179 ++
 .pine/tickets/BUG-qq4xva.md                        |   57 +
 .pine/tickets/BUG-rjd6fm.md                        |  272 ++
 .pine/tickets/BUG-rrkjrd.md                        |   76 +
 .pine/tickets/BUG-s0wy50.md                        |   56 +
 .pine/tickets/BUG-t2wezf.md                        |   85 +
 .pine/tickets/BUG-tcqkad.md                        |  188 ++
 .pine/tickets/BUG-txc9xg.md                        |   71 +
 .pine/tickets/BUG-wdypd2.md                        |  230 ++
 .pine/tickets/BUG-wp2y0y.md                        |   45 +
 .pine/tickets/BUG-xf1wqm.md                        |   42 +
 .pine/tickets/BUG-y57cz4.md                        |  167 ++
 .pine/tickets/BUG-ysvmaa.md                        |  298 +++
 .pine/tickets/BUG-ze1nn8.md                        |  114 +
 .pine/tickets/BUG-ztzxck.md                        |   56 +
 .pine/tickets/EPIC-cfe7ny.md                       |   39 +
 .pine/tickets/FEAT-0895qc.md                       |  311 +++
 .pine/tickets/FEAT-15k49d.md                       |   37 +
 .pine/tickets/FEAT-56nep4.md                       |  175 ++
 .pine/tickets/FEAT-cwmw90.md                       |   21 +
 .pine/tickets/FEAT-edxxj7.md                       |  188 ++
 .pine/tickets/FEAT-j5s2n4.md                       |  188 ++
 .pine/tickets/FEAT-jvembs.md                       |  363 +++
 .pine/tickets/FEAT-nqpvf6.md                       |  161 ++
 .pine/tickets/FEAT-qdedm0.md                       |   23 +
 .pine/tickets/FEAT-x5km1z.md                       |  142 ++
 Dockerfile                                         |   15 +-
 Makefile                                           |  114 +-
 README.md                                          |   27 +-
 cmd/kilasflow/main.go                              |  158 +-
 cmd/kilasflow/retention_test.go                    |   87 +
 compose.postgres.yaml                              |    9 +-
 compose.yaml                                       |   16 +-
 config.example.yaml                                |   13 +-
 devbox.json                                        |    2 -
 docs/astro.config.mjs                              |    4 +-
 docs/src/content/docs/404.md                       |    2 +-
 docs/src/content/docs/concepts/execution-model.md  |  210 +-
 docs/src/content/docs/concepts/expressions.md      |   99 +-
 docs/src/content/docs/concepts/node-registry.md    |   35 +-
 .../src/content/docs/concepts/safety-boundaries.md |  119 +-
 docs/src/content/docs/concepts/webhooks.md         |   62 +-
 docs/src/content/docs/guides/community-nodes.md    |    2 +-
 docs/src/content/docs/guides/embedding.md          |   16 +-
 docs/src/content/docs/guides/n8n-migration.md      |  127 +-
 docs/src/content/docs/guides/node-authoring.md     |   10 +-
 docs/src/content/docs/index.mdx                    |    2 +-
 .../docs/operate/configuration-reference.md        |   13 +-
 docs/src/content/docs/operate/deployment.md        |   26 +-
 docs/src/content/docs/operate/security.md          |   52 +-
 docs/src/content/docs/operate/upgrades.md          |   17 +-
 docs/src/content/docs/reference/api-contract.md    |   13 +-
 docs/src/content/docs/reference/api.md             |    6 +-
 docs/src/content/docs/reference/api/auth.md        |   11 +-
 docs/src/content/docs/reference/api/credentials.md |    2 +-
 docs/src/content/docs/reference/api/datastores.md  |  392 +++
 docs/src/content/docs/reference/api/embed.md       |    6 +-
 docs/src/content/docs/reference/api/executions.md  |    4 +-
 docs/src/content/docs/reference/api/interop.md     |    4 +-
 docs/src/content/docs/reference/api/nodes.md       |    2 +-
 docs/src/content/docs/reference/api/schedules.md   |   11 +-
 docs/src/content/docs/reference/api/system.md      |    4 +-
 docs/src/content/docs/reference/api/tenants.md     |  200 ++
 docs/src/content/docs/reference/api/workflows.md   |   12 +-
 .../content/docs/reference/expression-grammar.md   |  369 ++-
 docs/src/content/docs/start/install.md             |   44 +-
 docs/src/content/docs/start/what-kilasflow-is.md   |   91 +-
 e2e/fixtures/datastore.ts                          |    8 +-
 e2e/fixtures/epic-telegram.ts                      |   24 +-
 e2e/fixtures/gates.ts                              |   31 +
 e2e/fixtures/library-import.ts                     |   13 +-
 e2e/fixtures/n8n-live.ts                           |   10 +-
 e2e/playwright.config.ts                           |    4 +
 e2e/scripts/skip-budget.mjs                        |  107 +
 e2e/skip-budget.json                               |   25 +
 e2e/tests/ai-agent-ollama.spec.ts                  |   30 +-
 e2e/tests/epic-acceptance.spec.ts                  |    8 +-
 e2e/tests/library-import.spec.ts                   |    8 +-
 e2e/tests/waha-migration.spec.ts                   |    6 +-
 internal/ai/agent.go                               |  101 +-
 internal/ai/ai.go                                  |   31 +-
 internal/ai/ai_test.go                             |  132 +-
 internal/ai/memory.go                              |   21 +
 internal/ai/openai.go                              |  140 +-
 internal/ai/openai_test.go                         |  145 ++
 internal/ai/outputschema.go                        |   16 +
 internal/api/cors_test.go                          |  102 +
 internal/api/credentials_pagination_test.go        |   76 +
 internal/api/csv_export_test.go                    |   66 +
 internal/api/embed_confinement_test.go             |  284 +++
 internal/api/handlers/admin.go                     |  524 ++++
 internal/api/handlers/admin_admin_test.go          |  579 +++++
 internal/api/handlers/auth.go                      |  233 +-
 internal/api/handlers/auth_test.go                 |  366 +++
 internal/api/handlers/credentials.go               |   37 +-
 internal/api/handlers/datastores.go                |   32 +-
 internal/api/handlers/datastores_csv.go            |   72 +
 internal/api/handlers/datastores_csv_test.go       |  147 ++
 internal/api/handlers/embed.go                     |   14 +-
 internal/api/handlers/embedscope.go                |  126 +
 internal/api/handlers/executions.go                |  318 ++-
 internal/api/handlers/interop.go                   |  130 +-
 internal/api/handlers/problem.go                   |   31 +
 internal/api/handlers/schedules.go                 |   35 +-
 internal/api/handlers/workflows.go                 |  175 +-
 internal/api/handlers/workflows_conflict_test.go   |  125 +
 internal/api/handlers/workflows_delete_test.go     |    4 +-
 internal/api/import_diagnostics_test.go            |  142 ++
 internal/api/list_pagination_test.go               |  221 ++
 internal/api/middleware/auth.go                    |   82 +-
 internal/api/middleware/auth_test.go               |  436 ++++
 internal/api/middleware/clientip.go                |   50 +
 internal/api/middleware/cors.go                    |  132 +
 internal/api/middleware/cors_test.go               |  189 ++
 internal/api/middleware/loginlimit.go              |  203 ++
 internal/api/middleware/loginlimit_test.go         |  172 ++
 internal/api/middleware/sessioncache.go            |  156 ++
 internal/api/openapi_security_test.go              |  196 ++
 internal/api/routes.go                             |   14 +-
 internal/api/server.go                             |  123 +-
 internal/api/workflows_test.go                     |   88 +-
 internal/auth/auth_test.go                         |   42 +-
 internal/auth/session.go                           |   94 +-
 internal/auth/session_internal_test.go             |   49 +
 internal/binary/binary.go                          |   15 +-
 internal/conditions/conditions.go                  |  455 +++-
 internal/conditions/conditions_test.go             |  195 ++
 internal/conditions/doc.go                         |    8 +
 internal/config/boot_strictness_test.go            |  257 ++
 internal/config/config.go                          |  305 ++-
 internal/credentials/redirect_test.go              |  141 ++
 internal/credentials/registry.go                   |   16 +-
 internal/database/migrate.go                       |   59 +-
 internal/database/migrate_test.go                  |   45 +
 internal/datastore/catalogue.go                    |  154 +-
 internal/datastore/catalogue_test.go               |  115 +
 internal/datastore/fleet.go                        |    9 +-
 internal/embed/confinement.go                      |  156 ++
 internal/embed/confinement_test.go                 |  142 ++
 internal/embed/embed.go                            |   16 +
 internal/engine/approval.go                        |   17 +-
 internal/engine/authenticate.go                    |   45 +-
 internal/engine/authenticate_test.go               |  135 +
 internal/engine/checkpoint.go                      |   24 +
 internal/engine/error_workflow_test.go             |  230 ++
 internal/engine/expression_context_test.go         |   71 +
 internal/engine/lease_test.go                      |  401 +++
 internal/engine/live_progress_test.go              |  187 ++
 internal/engine/loopstate_test.go                  |  183 ++
 internal/engine/multiprocess_test.go               |  115 +-
 internal/engine/runner.go                          | 1670 +++++++++----
 internal/engine/runner_test.go                     | 1473 ++++++++++-
 internal/engine/service.go                         |  724 +++++-
 internal/engine/service_test.go                    |  385 ++-
 internal/engine/subworkflow_test.go                |    2 +-
 internal/engine/trace_persist_test.go              |  159 ++
 internal/engine/trace_test.go                      |    8 +-
 internal/engine/wait_service.go                    |  174 +-
 internal/engine/wait_service_test.go               |  232 +-
 internal/engine/worker_test.go                     |   15 +
 internal/expression/doc.go                         |   94 +-
 internal/expression/evaluator.go                   |  771 ++++++
 internal/expression/expression.go                  |  474 +---
 internal/expression/expression_test.go             |   56 +-
 internal/expression/functions.go                   |  219 --
 internal/expression/globals.go                     |  528 ++++
 internal/expression/luxon.go                       |  320 +++
 internal/expression/methods.go                     | 1187 +++++++++
 internal/expression/parity_test.go                 |  505 ++++
 internal/expression/parser.go                      |  824 +++++++
 internal/expression/roots.go                       |  339 ++-
 internal/interop/n8n/corpus/BASELINE.md            |   16 +-
 internal/interop/n8n/corpus/baseline.json          |   43 +-
 internal/interop/n8n/gowa.go                       |   37 +-
 internal/interop/n8n/gowa_test.go                  |   17 +-
 internal/interop/n8n/importer_tail_test.go         |  772 ++++++
 internal/interop/n8n/n8n.go                        |  462 +++-
 internal/interop/n8n/n8n_test.go                   |  177 +-
 internal/interop/n8n/parameters.go                 | 2606 ++++++++++++++++++--
 internal/interop/n8n/trivialcode.go                |  107 -
 internal/interop/n8n/trivialcode_test.go           |  109 -
 internal/interop/n8n/waitsubworkflow_test.go       |  460 ++++
 internal/loadoptions/loadoptions.go                |   11 +
 internal/loadoptions/redirect_test.go              |  117 +
 internal/property/property.go                      |   20 +-
 internal/property/visibility_test.go               |   34 +
 internal/repository/auth.go                        |  388 +++
 internal/repository/auth_admin_test.go             |  477 ++++
 internal/repository/claim_lease_test.go            |  266 ++
 internal/repository/claim_wake_test.go             |    4 +-
 internal/repository/credentials.go                 |  103 +
 internal/repository/execution_retention_test.go    |    3 +-
 internal/repository/executions.go                  |  534 +++-
 internal/repository/import_diagnostics.go          |   83 +
 internal/repository/import_diagnostics_test.go     |  199 ++
 internal/repository/models.go                      |   40 +-
 internal/repository/models_test.go                 |  141 +-
 internal/repository/postgres_execution_test.go     |    4 +-
 internal/repository/prefix_test.go                 |    8 +-
 internal/repository/schedule_list_test.go          |  188 ++
 internal/repository/schedules.go                   |  112 +
 internal/repository/subworkflow_activation_test.go |  153 ++
 internal/repository/tenant_purge_test.go           |    2 +-
 internal/repository/waits_test.go                  |    2 +-
 internal/repository/webhooks.go                    |  188 +-
 internal/repository/webhooks_test.go               |  306 +++
 internal/repository/workflow_history.go            |   14 +-
 internal/repository/workflow_list_test.go          |  196 ++
 internal/repository/workflows.go                   |  233 +-
 internal/safehttp/safehttp.go                      |   54 +-
 internal/safehttp/safehttp_test.go                 |   97 +
 internal/scheduler/doc.go                          |   13 +-
 internal/scheduler/extract.go                      |   52 +
 internal/scheduler/scheduler_test.go               |   67 +
 internal/sqlbuild/sqlbuild_test.go                 |    4 +-
 internal/web/dist/index.html                       |   37 -
 internal/web/embed.go                              |  459 +++-
 internal/web/embed_test.go                         |  335 ++-
 internal/web/placeholder/index.html                |   17 +
 internal/webhook/export_test.go                    |    2 +-
 internal/webhook/form.go                           |  262 ++
 internal/webhook/form_test.go                      |  169 ++
 internal/webhook/request_lifecycle.go              |  425 +++-
 internal/webhook/request_lifecycle_test.go         |  411 +++
 internal/webhook/shape.go                          |  299 ++-
 internal/webhook/shape_test.go                     |  146 +-
 internal/webhook/webhook.go                        |  758 ++++--
 internal/webhook/webhook_test.go                   |  678 ++++-
 internal/workflow/compiler.go                      |   81 +-
 internal/workflow/document.go                      |    4 +
 .../000010_execution_reclaim_count.down.sql        |    7 +
 .../postgres/000010_execution_reclaim_count.up.sql |   17 +
 .../000011_webhook_path_label_index.down.sql       |   12 +
 .../000011_webhook_path_label_index.up.sql         |   22 +
 .../000012_workflow_import_diagnostics.down.sql    |    9 +
 .../000012_workflow_import_diagnostics.up.sql      |   29 +
 .../sqlite/000010_execution_reclaim_count.down.sql |    7 +
 .../sqlite/000010_execution_reclaim_count.up.sql   |   17 +
 .../000011_webhook_path_label_index.down.sql       |   12 +
 .../sqlite/000011_webhook_path_label_index.up.sql  |   22 +
 .../000012_workflow_import_diagnostics.down.sql    |    9 +
 .../000012_workflow_import_diagnostics.up.sql      |   24 +
 nodes/ai.go                                        |  498 +++-
 nodes/ai_test.go                                   |  733 +++++-
 nodes/assignments.go                               |   51 +-
 nodes/core.go                                      |    5 +
 nodes/datastore.go                                 |   41 +-
 nodes/datastore_test.go                            |  228 +-
 nodes/datetime.go                                  |   80 +-
 nodes/datetime_test.go                             |  161 +-
 nodes/embedscope.go                                |  220 ++
 nodes/embedscope_test.go                           |  242 ++
 nodes/error_workflow.go                            |  223 ++
 nodes/error_workflow_test.go                       |  118 +
 nodes/executors.go                                 |   34 +-
 nodes/executors_test.go                            |   66 +
 nodes/http.go                                      |  347 ++-
 nodes/http_test.go                                 |  302 ++-
 nodes/loop.go                                      |  160 +-
 nodes/sql_options_live_test.go                     |   33 +-
 nodes/subworkflow.go                               |   97 +-
 nodes/subworkflow_calls_test.go                    |   56 +
 nodes/telegram_download.go                         |    3 +-
 nodes/telegram_lifecycle.go                        |    7 +-
 nodes/transform.go                                 |   32 +-
 nodes/transform_test.go                            |    8 +-
 nodes/unsupported.go                               |   16 +
 nodes/wait.go                                      |  217 +-
 nodes/webhook.go                                   |  477 +++-
 packs/waha/README.md                               |   22 +
 packs/waha/manifest-202409.json                    |   11 +-
 packs/waha/manifest-202502.json                    |   11 +-
 packs/waha/pack-trigger-202409.json                |   11 +-
 packs/waha/pack-trigger-202502.json                |   11 +-
 packs/waha/waha.go                                 |   83 +-
 packs/waha/waha_test.go                            |  373 ++-
 packs/waha/webhook-lifecycle.json                  |   14 +
 scripts/check-coordinates.sh                       |   60 +
 scripts/generate-api-reference.mjs                 |   30 +-
 scripts/smoke-dev.sh                               |   27 +
 sdk/README.md                                      |   12 +
 sdk/examples/host-page/README.md                   |   11 +-
 sdk/examples/reference-host/README.md              |  122 +-
 sdk/examples/reference-host/package.json           |    2 +-
 sdk/examples/reference-host/server.mjs             |   29 +-
 sdk/package.json                                   |    6 +-
 web/src/lib/api/generated/admin/admin.ts           |  957 +++++++
 web/src/lib/api/generated/auth/auth.ts             |   32 +-
 .../lib/api/generated/credentials/credentials.ts   |   30 +-
 web/src/lib/api/generated/datastores/datastores.ts |   32 +-
 web/src/lib/api/generated/embed/embed.ts           |    2 +-
 web/src/lib/api/generated/executions/executions.ts |    2 +-
 web/src/lib/api/generated/interop/interop.ts       |  115 +-
 .../models/createTenantAPIKeyInputBody.ts          |   17 +
 .../api/generated/models/createTenantInputBody.ts  |   24 +
 .../generated/models/createTenantUserInputBody.ts  |   29 +
 .../lib/api/generated/models/embedSessionBody.ts   |   11 +-
 .../api/generated/models/embedSessionResource.ts   |    1 +
 .../generated/models/executionRequestResource.ts   |    1 +
 web/src/lib/api/generated/models/index.ts          |   15 +
 .../lib/api/generated/models/listApiKeysParams.ts  |   20 +
 .../api/generated/models/listCredentialsParams.ts  |   20 +
 .../api/generated/models/listDatastoresParams.ts   |   20 +
 .../api/generated/models/listSchedulesParams.ts    |   20 +
 .../generated/models/listTenantUsersOutputBody.ts  |   15 +
 .../api/generated/models/listTenantsOutputBody.ts  |   15 +
 .../api/generated/models/listWorkflowsParams.ts    |   20 +
 web/src/lib/api/generated/models/node.ts           |    1 +
 .../api/generated/models/runWorkflowInputBody.ts   |    2 +
 .../generated/models/setUserPasswordInputBody.ts   |   18 +
 web/src/lib/api/generated/models/tenantResource.ts |   17 +
 web/src/lib/api/generated/models/userResource.ts   |   18 +
 .../generated/models/workflowDiagnosticsParams.ts  |   14 +
 .../models/workflowDiagnosticsResource.ts          |   20 +
 .../api/generated/models/workflowDocumentInput.ts  |    2 +
 web/src/lib/api/generated/schedules/schedules.ts   |   32 +-
 .../workflow-lifecycle/workflow-lifecycle.ts       |    2 +-
 web/src/lib/api/generated/workflows/workflows.ts   |   32 +-
 web/src/lib/api/http.ts                            |   16 +
 .../lib/components/ui/sheet/sheet-content.svelte   |   11 +-
 .../workflow-editor/canvas-bridge.svelte           |   39 +
 .../components/workflow-editor/canvas-edge.svelte  |   81 +
 .../components/workflow-editor/canvas-node.svelte  |  376 ++-
 .../components/workflow-editor/node-picker.svelte  |  148 +-
 .../workflow-editor/properties-panel.svelte        |  167 +-
 .../workflow-editor/property-field.svelte          |  534 ++--
 .../workflow-editor/version-panel.svelte           |   62 +-
 .../workflow-editor/workflow-editor.svelte         |  736 +++++-
 web/src/lib/dashboard/execution-list.test.ts       |  102 +
 web/src/lib/dashboard/execution-list.ts            |  106 +
 web/src/lib/dashboard/workflow-list.test.ts        |  101 +
 web/src/lib/dashboard/workflow-list.ts             |  122 +
 web/src/lib/embed/embed-editor.svelte              |  151 +-
 web/src/lib/embed/session.svelte.ts                |   73 +-
 web/src/lib/embed/session.test.ts                  |   66 +-
 web/src/lib/type-guards.ts                         |   13 +
 web/src/lib/workflow-editor/authoring.test.ts      |  118 +
 web/src/lib/workflow-editor/canvas-actions.ts      |   14 +
 web/src/lib/workflow-editor/catalog.test.ts        |   99 +
 web/src/lib/workflow-editor/catalog.ts             |   99 +
 web/src/lib/workflow-editor/clipboard.test.ts      |  203 ++
 web/src/lib/workflow-editor/clipboard.ts           |  293 +++
 web/src/lib/workflow-editor/conditions.test.ts     |   94 +-
 web/src/lib/workflow-editor/conditions.ts          |  176 +-
 web/src/lib/workflow-editor/credentials.test.ts    |   39 +
 web/src/lib/workflow-editor/credentials.ts         |   29 +-
 web/src/lib/workflow-editor/document.test.ts       |  158 ++
 web/src/lib/workflow-editor/document.ts            |  291 ++-
 web/src/lib/workflow-editor/event-stream.svelte.ts |   28 +-
 web/src/lib/workflow-editor/event-stream.test.ts   |   21 +-
 web/src/lib/workflow-editor/execution.test.ts      |   16 +
 web/src/lib/workflow-editor/execution.ts           |    3 +-
 .../lib/workflow-editor/expression-assist.test.ts  |   50 +
 web/src/lib/workflow-editor/expression-assist.ts   |   91 +
 .../lib/workflow-editor/expression-grammar.test.ts |   49 +
 web/src/lib/workflow-editor/expression-grammar.ts  |   49 +
 web/src/lib/workflow-editor/history.test.ts        |   68 +
 web/src/lib/workflow-editor/history.ts             |   93 +
 .../lib/workflow-editor/import-diagnostics.test.ts |   41 +
 web/src/lib/workflow-editor/import-diagnostics.ts  |   92 +
 web/src/lib/workflow-editor/layout.test.ts         |  108 +
 web/src/lib/workflow-editor/layout.ts              |  257 +-
 web/src/lib/workflow-editor/loader-cache.test.ts   |  116 +
 web/src/lib/workflow-editor/loader-cache.ts        |   97 +
 web/src/lib/workflow-editor/node-visual.ts         |   12 +
 web/src/lib/workflow-editor/parameter.test.ts      |   21 +-
 web/src/lib/workflow-editor/parameter.ts           |   16 +
 web/src/lib/workflow-editor/ports.test.ts          |  243 +-
 web/src/lib/workflow-editor/ports.ts               |  125 +-
 web/src/lib/workflow-editor/run-trigger.test.ts    |   75 +
 web/src/lib/workflow-editor/run-trigger.ts         |   39 +
 web/src/lib/workflow-editor/shortcuts.test.ts      |   77 +
 web/src/lib/workflow-editor/shortcuts.ts           |  115 +
 web/src/lib/workflow-editor/sticky.test.ts         |   37 +
 web/src/lib/workflow-editor/sticky.ts              |   75 +
 web/src/lib/workflow-editor/validation.test.ts     |   41 +-
 web/src/lib/workflow-editor/validation.ts          |   38 +-
 web/src/lib/workflow-editor/visibility.ts          |   10 +-
 web/src/lib/workflow-editor/workflow-cache.test.ts |   46 +
 web/src/lib/workflow-editor/workflow-cache.ts      |   33 +
 web/src/routes/(dashboard)/+layout.svelte          |   32 +-
 .../routes/(dashboard)/app/workflows/+page.svelte  |  369 ++-
 .../(dashboard)/app/workflows/[id]/+page.svelte    |  369 ++-
 .../app/workflows/diagnostics-section.svelte       |   10 +-
 .../(dashboard)/app/workflows/import-dialog.svelte |   28 +-
 .../app/workflows/import-report-drawer.svelte      |   60 +
 .../(dashboard)/app/workflows/import-report.svelte |  122 +-
 .../routes/(dashboard)/credentials/+page.svelte    |  147 +-
 web/src/routes/(dashboard)/datastores/+page.svelte |    6 +-
 .../(dashboard)/datastores/[id]/+page.svelte       |  168 +-
 web/src/routes/(dashboard)/executions/+page.svelte |  176 +-
 .../(dashboard)/executions/[id]/+page.svelte       |  223 +-
 web/src/routes/(dashboard)/schedules/+page.svelte  |   59 +-
 web/src/routes/(dashboard)/settings/+page.svelte   |  293 ++-
 web/src/routes/embed/[id]/+page.svelte             |   12 +-
 web/src/routes/login/+page.svelte                  |   75 +
 web/vite.config.ts                                 |    7 +-
 434 files changed, 56258 insertions(+), 4725 deletions(-)
```

## Reopened by review (2026-09-20)
ReviewExpression (b4f2147..HEAD) found 11 defects in the rewritten engine, all reproduced:
- **P2 regression**: `{{ $input }}`, `{{ JSON }}` (and Object/Array/Math/DateTime/Number/String/Boolean) and a lineage error embedded in a container return unexported evaluator types that marshal to `{}` — a Set node assigned `{{ $input }}` writes `{}` where the old `rootValue` wrote the port map; `{{ JSON.stringify($input) }}` yields `"{}"`; `{{ $('Many').item }}` inside an array/object drops its refusal.
- **P2**: the `{{ }}` splitter cuts at the first `}}` without lexer rules, so `{{ JSON.stringify({a:{b:1}}) }}` and `{{ JSON.parse('{"a":{"b":1}}') }}` fail with a message blaming the expression.
- **P2**: `==`/`!=` skip abstract-equality coercion for booleans and collections (`true == 1`, `[] == false`, `[1,2] == '1,2'` all false).
- **P2**: `Object.keys/values/entries` sort list indices textually (`Object.values` of an 11-element list returns `[0,1,10,2,…]`).
- **P2**: `JSON.stringify({a: undefined, b: 1})` emits `{"a":null,"b":1}` where JS drops the key.
- **P3**: `sort(comparator)` accepts and ignores the comparator (`[10,9,100].sort((a,b)=>a-b)` → `[10,100,9]`); `[1,2,3]['length']` is undefined while `.length` is 3; `Math.round(-2.5)` is -3 (JS -2); `(2.5).toFixed(0)` is "2" (JS "3", ties-to-even vs half-up); `1e-7 + ''` renders `1e-07` (JS `1e-7`); `JSON.stringify(Infinity)`/a bare non-finite value fails the execution with a Go marshal message (JS encodes null).
